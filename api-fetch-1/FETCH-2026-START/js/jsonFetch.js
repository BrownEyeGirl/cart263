/**
 * JSON => Javascrip Object Notation. datainterchange format
 * 
 * used to store local data or transmit web data
 * its a string, needs to be PARSED (converted to JS object) to access data
 * 
 */

window.onload = goApp;

async function goApp(){
console.log("hello fetch")

}

